[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dm_bot", "career_mgr", "email_sync"]

[tool.uv.sources]
agent-py = { git = "ssh://git@github.com/faiz95ahmed/agent-py.git" }

[project]
name = "linkedin-dm-bot"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "sqlite-utils>=3.39",
    "playwright>=1.40.0",
    "typer>=0.9.0",
    "requests>=2.32.5",
    "trafilatura>=2.0.0",
]

[project.scripts]
dm-bot = "dm_bot.main:main"
career = "career_mgr.main:main"
email-sync = "email_sync.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "hypothesis>=6.92.0",
]

[dependency-groups]
dev = [
    "agent-py",
    "mypy>=1.19.1",
]
